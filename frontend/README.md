# Rex AI

To run this locally, install dependencies and start the dev server:

```bash
npm install
npm run dev
npm run api
```

Then open the local URL printed in your terminal (usually `http://localhost:5173`).
